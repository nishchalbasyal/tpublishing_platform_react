import SettingsIcon from '@mui/icons-material/Settings';


const Settingsrolling = ({color,design}) => {
  return (
    <span className='animate'>
      <SettingsIcon id="element" style={{color:color}} />
    </span>
  )
}

export default Settingsrolling
